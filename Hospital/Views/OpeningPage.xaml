<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Hospital.ViewModels"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="Hospital.Views.OpeningPage"
             x:DataType="vm:OpeningViewModel">

    <StackLayout>
        <zxing:CameraBarcodeReaderView x:Name="cameraView"
                                       BarcodesDetected="cameraView_BarcodesDetected"
                                       HorizontalOptions="FillAndExpand"
                                       VerticalOptions="FillAndExpand"
                                       WidthRequest="150"
                                       HeightRequest="150"/>
        <Button Margin="0,50,0,200" Command="{Binding PrintTextClickedCommand}" Text="SIGN UP" BackgroundColor="LightGreen" TextColor="White" FontAttributes="Bold" HeightRequest="50" CornerRadius="30" WidthRequest="200" FontSize="Small"/>

        <Entry Text="{Binding ScannedText}"
               IsReadOnly="True"
               TranslationX="250"
               IsVisible="Hidden"
               x:Name="text"
               HorizontalOptions="Center"
               VerticalOptions="End"
               Margin="10"/>
    </StackLayout>

</ContentPage>
